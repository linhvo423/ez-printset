# App Icon

Put the Windows icon file here:

```txt
assets/app.ico
```

Recommended icon sizes inside the `.ico` file:

- 16 x 16
- 32 x 32
- 48 x 48
- 256 x 256

After replacing `assets/app.ico`, rebuild the app with GitHub Actions or `scripts/build_exe.ps1`.
